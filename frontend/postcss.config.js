/* postcss.config.js
   PostCSS processes your CSS. Tailwind uses it as a plugin.
   autoprefixer adds vendor prefixes (-webkit-, etc.) for browser compatibility. */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
